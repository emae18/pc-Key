#incluide<bits.stdc++.h>
#define forin(i,n) for(int i=0;i<(int)n;i++)
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin>>n;
    while(n--){
        int n,sum;
        cin>>n>>sum;
        
    }    
    
    return 0;
}