#include<bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
 	cin.tie(NULL);
    int n,a,b,c;
    cin>>n;
    multiset<int,less<int> > notas;
    while(n--)
    {
        cin>>
    }
    return 0;
}
