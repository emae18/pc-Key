#include<bits/stdc++.h>

using namespace std;

int main()
{
    int n,t;string aux;
    cin>>n;vector<string> v;
    while(n--)
    {
        cin>>t;
        int v2[t]={0};
        for(int i=0;i<t;i++)
        {
            cin>>aux;
            if(find(v.begin(),v.end(),aux)!=v.end())
            {
                v.
            }
        }
    }
    return 0;
}
