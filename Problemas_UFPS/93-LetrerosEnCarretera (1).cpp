#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
 	cin.tie(NULL);
 	cout.tie(NULL);
    int n;string aux;
    while(cin>>n)
    {
        int v[n];
        for(int i=0;i<n;i++)
        {
            cin>>v[i];
        }

    }
    return 0;
}
