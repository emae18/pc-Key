#include<bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin>>n;
    long long v[n];
    for(int i=0;i<n;i++)
        cin>>v[i];
    long long k=v[0];
    int cont=0;
    int tam;
    bool ord=false;
    while(!ord)
    {

    }


    return 0;
}
