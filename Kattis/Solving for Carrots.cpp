#include<bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n,p;
    cin>>n>>p;string aux;
    cin.ignore();
    while(n--)
    {
        getline(cin,aux);
    }
    cout<<p<<"\n";
    return 0;
}
